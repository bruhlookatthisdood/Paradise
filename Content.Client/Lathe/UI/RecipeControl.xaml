<Control xmlns="https://spacestation14.io" StyleClasses="RecipeControl">
    <Button
        Name="Button"
        HorizontalExpand="True"
        Margin="0"
        StyleClasses="transparentButton">
        <BoxContainer Orientation="Horizontal">
            <BoxContainer
                Name="RecipeDisplayContainer"
                Margin="0 0 4 0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MinSize="32 32"
                />
            <Label Name="RecipeName" HorizontalExpand="True" />
        </BoxContainer>
    </Button>
</Control>
