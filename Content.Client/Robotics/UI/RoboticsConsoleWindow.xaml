<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      Title="{Loc 'robotics-console-window-title'}"
                      MinSize="600 450">
    <BoxContainer Orientation="Horizontal">
        <!-- List of borgs -->
        <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True" Margin="8" SizeFlagsStretchRatio="0.5">
            <controls:StripeBack HorizontalExpand="True" MinHeight="36" HasBottomEdge="False" HasTopEdge="False"
                                 StyleClasses="WarningStripe" Name="NoCyborgs">
                <PanelContainer HorizontalAlignment="Center" VerticalAlignment="Center" StyleClasses="SurfacePrimary">
                    <Label Text="{Loc 'robotics-console-no-cyborgs'}" HorizontalAlignment="Center"
                           VerticalAlignment="Center" Margin="8 6" />
                </PanelContainer>
            </controls:StripeBack>
            <ScrollContainer Name="CyborgsContainer" VerticalExpand="True" Visible="False">
                <!-- Populated when loading state -->
                <ItemList Name="Cyborgs" />
            </ScrollContainer>
        </BoxContainer>

        <!-- Selected borg info -->
        <PanelContainer StyleClasses="SurfacePrimary" HorizontalExpand="True" VerticalExpand="True" Margin="8">
        <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True">
            <controls:StripeBack HorizontalExpand="True" MinHeight="36" HasBottomEdge="False" HasTopEdge="False"
                                 StyleClasses="WarningStripe" Name="SelectCyborg">
                <PanelContainer HorizontalAlignment="Center" VerticalAlignment="Center" StyleClasses="SurfacePrimary">
                    <Label Text="{Loc 'robotics-console-select-cyborg'}" HorizontalAlignment="Center"
                           VerticalAlignment="Center" Margin="8 6" />
                </PanelContainer>
            </controls:StripeBack>
            <BoxContainer Name="BorgContainer" Orientation="Vertical" MaxHeight="200" Visible="False">
                <BoxContainer Margin="5 5 5 5" Orientation="Horizontal">
                    <PanelContainer VerticalExpand="True">
                        <BoxContainer HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextureRect Name="BorgSprite" TextureScale="4 4" />
                        </BoxContainer>
                    </PanelContainer>
                    <PanelContainer VerticalExpand="True" HorizontalExpand="True">
                        <RichTextLabel Name="BorgInfo" />
                    </PanelContainer>
                    <!-- TODO: button to open camera window for this borg -->
                </BoxContainer>
                <controls:StripeBack HasBottomEdge="False" HasTopEdge="False"
                                     StyleClasses="DangerStripe">
                    <BoxContainer Name="DangerZone" Margin="5" Orientation="Horizontal" HorizontalExpand="True"
                                  HorizontalAlignment="Center" Visible="False">
                        <Button Name="DisableButton" Text="{Loc 'robotics-console-disable'}" StyleClasses="OpenRight" />
                        <Button Name="DestroyButton" Text="{Loc 'robotics-console-destroy'}" StyleClasses="OpenLeft" />
                    </BoxContainer>
                    <Label Name="LockedMessage" Text="{Loc 'robotics-console-locked-message'}" HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
                </controls:StripeBack>
            </BoxContainer>
        </BoxContainer>
        </PanelContainer>
    </BoxContainer>
</controls:FancyWindow>
