<controls:FancyWindow xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    Title="{Loc 'comms-console-menu-title'}"
    MinSize="450 300">

    <!-- Main Container -->
    <BoxContainer Orientation="Vertical"
        HorizontalExpand="False"
        VerticalExpand="True"
        Margin="6 6 6 5">

        <!-- ButtonsPart -->
        <BoxContainer Orientation="Vertical"
            VerticalAlignment="Bottom"
            SeparationOverride="4"
            Margin="8 2">
            <PanelContainer StyleClasses="SurfacePrimary">
            <!-- AnnouncePart -->
            <BoxContainer Orientation="Vertical"
                Margin="8" SeparationOverride="5">

                <Label Text="{Loc 'comms-console-menu-announcement-section-header'}" StyleClasses="SectionHeader" Margin="0 4 0 0"/>

                <PanelContainer StyleClasses="SeperatorAccented" SetHeight="2" HorizontalExpand="True" Margin="0 0 0 2"/>

                <Button Name="AnnounceButton"
                        Access="Public"
                        Text="{Loc 'comms-console-menu-announcement-button'}"
                        ToolTip="{Loc 'comms-console-menu-announcement-button-tooltip'}"
                        StyleClasses="OpenLeft"
                        Margin="0 0 0 0"
                        Disabled="True"/>

                <Button Name="BroadcastButton"
                    Access="Public"
                    Text="{Loc 'comms-console-menu-broadcast-button'}"
                    ToolTip="{Loc 'comms-console-menu-broadcast-button-tooltip'}"
                    StyleClasses="OpenBoth"/>

                <controls:TguiOptionButton Name="AlertLevelButton"
                                           Access="Public"
                                           ToolTip="{Loc 'comms-console-menu-alert-level-button-tooltip'}"
                                           StyleClasses="OpenRight"/>

            </BoxContainer>
            </PanelContainer>

            <PanelContainer StyleClasses="ListView" Margin="0 5">
            <TextEdit Name="MessageInput"
                      VerticalExpand="True"
                      HorizontalExpand="True"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      MinHeight="125" Margin="4"/>
            </PanelContainer>
            <!-- EmergencyPart -->
            <BoxContainer Orientation="Vertical"
                SeparationOverride="6" MinHeight="75">

                <RichTextLabel Name="CountdownLabel"/>

                <!-- DeltaV - Exfiltration Shuttle -->
                <RichTextLabel Name="ExfiltrationCountdownLabel"/>

                <Button Name="EmergencyShuttleButton"
                    Access="Public"
                    Text="Placeholder Text"
                    ToolTip="{Loc 'comms-console-menu-emergency-shuttle-button-tooltip'}"/>

                <!-- DeltaV - Exfiltration Shuttle -->
                <Button Name="ExfiltrationShuttleButton"
                    Access="Public"
                    Text="Placeholder Text"
                    ToolTip="{Loc 'comms-console-menu-exfiltration-shuttle-button-tooltip'}"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
