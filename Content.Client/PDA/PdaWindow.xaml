<pda:PdaWindow xmlns="https://spacestation14.io"
               xmlns:pda="clr-namespace:Content.Client.PDA"
               MouseFilter="Stop"
               MinHeight="600">
    <!-- The negative markin fixes a gap between the window edges and the decorative panel -->
    <PanelContainer StyleClasses="BackgroundPanel" HorizontalExpand="True" >
        <BoxContainer Orientation="Vertical" HorizontalExpand="True">
            <!--Heading-->

            <BoxContainer SetHeight="26" Orientation="Horizontal" Margin="8 2">
                <Label Text="PDA" StyleClasses="FancyWindowTitle"/>
                <Control HorizontalExpand="True" />
                <TextureButton Name="CloseButton" StyleClasses="windowCloseButton"
                               VerticalAlignment="Center" Margin="0 4 4 0" />
            </BoxContainer>

            <PanelContainer StyleClasses="LowDivider" Margin="0 2 0 0" />
            <!--Content-->
            <Control Margin="0 0" RectClipContent="True" VerticalExpand="true"
                     HorizontalExpand="True">
                <PanelContainer Name="ContentBorder" StyleClasses="PdaBackground" />
                <Control>
                    <BoxContainer Access="Public" Name="ContentsContainer" Orientation="Vertical"
                                  StyleClasses="PdaContent" />

                </Control>
            </Control>
        </BoxContainer>
    </PanelContainer>
</pda:PdaWindow>
